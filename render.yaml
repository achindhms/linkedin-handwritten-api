services:
  - type: web
    name: postcard-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: API_KEYS
        sync: false
      - key: RATE_LIMIT_PER_MINUTE
        value: "60"
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
